# Lightweight requirements for free tier deployment
# Render.com free tier has 512MB RAM limit

# FastAPI & Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# AI - OpenAI only (lightweight)
openai==1.6.1
tiktoken==0.5.2

# LangChain (minimal)
langchain==0.1.0
langchain-community==0.0.13
langchain-openai==0.0.5

# Vector Store - ChromaDB (lightweight)
chromadb==0.4.22

# Utilities
python-dotenv==1.0.0
httpx==0.26.0
numpy==1.24.3
